[user]
        name = Mathew Davies
        email = ThePixelDeveloper@users.noreply.github.com
[filter "lfs"]
        clean = git-lfs clean %f
        smudge = git-lfs smudge %f
        required = true
[push]
        default = simple
